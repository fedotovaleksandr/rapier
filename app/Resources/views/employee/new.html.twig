{% extends 'base.html.twig' %}

{% block body %}
    <h2>Employee creation</h2>

    {{ form_start(form) }}
        <div>
            {{ form_label(form.lastName, 'label.lastname') }}
            {{ form_errors(form.lastName) }}
            {{ form_widget(form.lastName, {'attr':{'class':'abcinput'}}) }}
        </div>
        <!-- TODO -->
        <input type="submit" value="action.save" />
    </form>

    <ul>
        <li>
            <a href="{{ path('employee_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
